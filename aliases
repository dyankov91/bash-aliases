# Load Generic Aliases
# -----------------------------------------------
if [ -f ~/.sh/generic-aliases ]; then
    . ~/.sh/generic-aliases
fi

# Load Git Aliases
# -----------------------------------------------
if [ -f ~/.sh/git-aliases ]; then
    . ~/.sh/git-aliases
fi

# Load Laravel Aliases
# -----------------------------------------------
if [ -f ~/.sh/laravel-aliases ]; then
    . ~/.sh/laravel-aliases
fi

# Load Docker Aliases
# -----------------------------------------------
if [ -f ~/.sh/docker-aliases ]; then
    . ~/.sh/docker-aliases
fi

# Load User Specific Aliases (.gitignored)
# -----------------------------------------------
if [ -f ~/.sh/user-aliases ]; then
    . ~/.sh/user-aliases
fi
